## One-time setup
# Copy to .env.local and fill in real values.
#   cp .env.tokens.template .env.local
#
# Required
# Generate token with: openssl rand -hex 32
DATA_ROOM_TOKEN=""
DATABASE_URL="file:./dev.db"

# Optional: separate token for outreach endpoint (falls back to DATA_ROOM_TOKEN)
OUTREACH_TOKEN=""

# Optional (AI engines)
ANTHROPIC_API_KEY=""

# Optional (behavior)
DEMO_MODE="false"
NODE_ENV="development"
