## Required
# Protects data-room ZIP export, audit PDF, and compliance endpoints.
# Generate with: openssl rand -hex 32
DATA_ROOM_TOKEN=""

# Prisma database connection string (Postgres example)
# Local Docker (from docker-compose.yml):
# DATABASE_URL="postgresql://bickford:YOUR_PASSWORD@localhost:5432/bickford?schema=public"
DATABASE_URL=""

## Optional (AI engines)
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""

## Optional (behavior)
# Safe presentation mode (redacts intents, offline Claude). Defaults to false.
DEMO_MODE="false"

## Bickford execution (founder-only)
# Hard gate: if not "true", /api/bickford/execute returns 403.
BICKFORD_EXECUTION_ENABLED="false"

# Founder-only irreversible execution guard.
# If unset, execution is blocked.
# Set this ONLY in your own machine / production secret store.
# Recommended: generate a long random value (e.g. `openssl rand -hex 32`).
BICKFORD_FOUNDER_KEY=""

# Local git operations (only if you run Bickford in a trusted environment)
BICKFORD_GIT_ENABLED="false"

# Auto-rollback does a force-push; keep off unless you explicitly want this.
BICKFORD_ROLLBACK_ENABLED="false"

## API Auth (recommended)
# In production, most write/compute endpoints require a bearer token unless you explicitly opt into a public API.
# Generate with: openssl rand -hex 32
BICKFORD_API_TOKEN=""

# Set true only if you intentionally want unauthenticated access in production.
BICKFORD_PUBLIC_API="false"

## Self-editing deployments (Netlify-safe)
# If GITHUB_TOKEN is set, Bickford will persist changes by creating a branch + PR via GitHub API.
# This is required when running from Netlify (serverless can't reliably run `git push`).
GITHUB_TOKEN=""
BICKFORD_GITHUB_REPO="bickfordd-bit/anthropic-acquisition"
BICKFORD_GITHUB_BASE_BRANCH="main"

## Optional: surface Netlify deploy preview URL (PR/branch)
NETLIFY_SITE_ID=""
NETLIFY_TOKEN=""

# Optional: manual trigger hook (used in local mode)
NETLIFY_BUILD_HOOK=""

## Optional (model overrides)
# Defaults: claude-3-5-sonnet-latest
ANTHROPIC_CHAT_MODEL=""
ANTHROPIC_INTENT_MODEL=""
ANTHROPIC_PLAN_MODEL=""

## Multi-Model Configuration
# Default model to use: "claude" | "gpt" | "auto" (default: "auto")
BICKFORD_DEFAULT_MODEL="auto"

# Enable automatic fallback to secondary model on failure (default: "true")
BICKFORD_ENABLE_FALLBACK="true"

# Model selection strategy: "cost" | "quality" | "speed" (default: "quality")
BICKFORD_MODEL_STRATEGY="quality"

# Optional: OpenAI model override (default: gpt-4-turbo-preview)
OPENAI_CHAT_MODEL=""

## Optional (automation / scripts)
# Used by health-check + remote export verification
BICKFORD_BASE_URL=""
HEALTHCHECK_URL=""

# If true, remote verification scripts fail when missing remote config
REQUIRE_REMOTE="false"

# If set, outreach endpoint can be protected separately from DATA_ROOM_TOKEN
OUTREACH_TOKEN=""

# CI-only: demo workflow can publish GitHub releases when true
PUBLISH_GITHUB_RELEASE="false"

# Usually set by the platform (Next/Vercel/Netlify)
NODE_ENV="development"
