# Netlify Environment Variables
# Copy these to Netlify Dashboard → Site configuration → Environment variables

DATA_ROOM_TOKEN=your-secure-token-here
BICKFORD_API_TOKEN=your-api-token-here
BICKFORD_PUBLIC_API=false

# Self-editing from the deployed URL (GitHub PR mode)
# Use a fine-grained PAT with repo contents + pull request write access (or a GitHub App token)
GITHUB_TOKEN=your-github-token-here
BICKFORD_GITHUB_REPO=bickfordd-bit/anthropic-acquisition
BICKFORD_GITHUB_BASE_BRANCH=main

# Optional: allow the UI to show a deploy-preview URL by polling Netlify deploys
# (Netlify creates deploy previews automatically on PRs/branches when enabled)
# NETLIFY_SITE_ID=...
# NETLIFY_TOKEN=...

# If you provision Netlify DB, Netlify will inject:
# - NETLIFY_DATABASE_URL
# - NETLIFY_DATABASE_URL_UNPOOLED
# This repo maps those to DATABASE_URL automatically (see lib/prisma.ts),
# so you usually do NOT need to set DATABASE_URL manually.
#
# DATABASE_URL=postgresql://...
ANTHROPIC_API_KEY=your-anthropic-key-here
DEMO_MODE=false
NODE_ENV=production

# Optional model overrides (defaults: claude-3-5-sonnet-latest)
# ANTHROPIC_CHAT_MODEL=claude-3-5-sonnet-latest
# ANTHROPIC_INTENT_MODEL=claude-3-5-sonnet-latest

# Optional: separate token for /api/outreach/trigger (falls back to DATA_ROOM_TOKEN)
# OUTREACH_TOKEN=your-outreach-token-here

# Optional: disable Next.js image optimization on Netlify
# NEXT_PUBLIC_DISABLE_IMAGE_OPTIMIZATION=true
